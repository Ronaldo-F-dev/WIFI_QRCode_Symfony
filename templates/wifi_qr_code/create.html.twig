{% extends 'base.html.twig' %}

{% block title %}Hello WifiQrCodeController!{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row mt-5">
            <div class="col-md-6 mx-auto mt-5">
            <h1 class="my-5">Générer le code QR de votre WIFI</h1>
                {{form_start(form)}}
                    {{form_widget(form)}}
                    <input type="submit" value="Générer" class="btn btn-success mt-2 w-100"/>
                {{form_end(form)}}
            </div>
        </div>
    </div>
{% endblock %}
